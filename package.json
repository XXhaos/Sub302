{
  "name": "sub302",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny Cloudflare Pages subscription redirect manager: UI + CRUD + 302 only.",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('Sub302 uses Cloudflare Pages Functions; no build step required.')\"",
    "dev": "wrangler pages dev public --kv SUB302_KV --compatibility-date=2026-05-10",
    "deploy": "wrangler pages deploy public --project-name sub302"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
